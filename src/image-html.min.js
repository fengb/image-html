var Pixels = require('./pixels');
var format = require('./formats/04');

var ImageHtml = module.exports = {
  formats: function(){
    return ['04'];
  },

  convert: function(image, id, format){
    var pixels = Pixels.fromImage(image);
    return format(pixels, id);
  }
};
